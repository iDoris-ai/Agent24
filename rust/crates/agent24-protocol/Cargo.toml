[package]
name = "agent24-protocol"
version = "0.3.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Agent24 v1 protocol types — locked to protocol/ via fixture tests (B1); becomes the generation source in B4"

[dependencies]
fs2 = "0.4"
serde = { workspace = true }
serde_json = { workspace = true }
schemars = { workspace = true }
# T7b/ME-3e: `approval_digest()` (design doc, decision 6) — sha256 of a
# canonicalized (key-sorted) payload, hex-encoded.
sha2 = "0.10"
hex = "0.4"
thiserror = "2"

[lints]
workspace = true
