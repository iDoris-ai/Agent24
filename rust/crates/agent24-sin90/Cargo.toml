[package]
name = "agent24-sin90"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Sin90 — Agent24 built-in Personal-OS domain: pure entities, state machines and Proposal validation (zero framework dependencies by design, ADR-026)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = "2"

[lints]
workspace = true
