[package]
name = "agent24-workspace"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Workspace service boundary and typed creation specifications"

[lints]
workspace = true

[target.'cfg(unix)'.dependencies]
rustix = { version = "0.38", features = ["fs"] }
