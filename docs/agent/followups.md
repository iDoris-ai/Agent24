# Follow-ups ledger（append-only · 永不删行 · 提交进仓库）

> pilot 的 review triage 把「真问题但不阻塞（B）」和延后项记在这里。
> 主线 task 全部完成后，由 `pilot run` 批量合成一个 cleanup PR 做掉，逐条标 [x] done=PR#n。
> `- [ ]`=OPEN，`- [x]`=DONE。GitHub PR comment 是永久兜底。
>
> **一条规则，学费是 FU-16（2026-09-02）**：
> **未做完的事必须由一条 `- [ ]` 承载，不能写在一条已关闭条目的正文里。**
> 「若将来发现 X 不足以覆盖，这条要重新打开」读起来像闸门，**实际是留言** ——
> 没有任何机制会再读它一次，`count-open` 也不会数它。
> 交付了 A 但原条目要的是 A+B 时：把原条目的范围**收窄到 A** 并标 done，
> **另开一条 `- [ ]` 承载 B**。两个数（`count-open`、`[x]` 与 `done=PR#n` 相等）
> 才会同时说真话。
>
> 判据用**宽**写法，不要加行尾锚：
> ```bash
> [ "$(grep -cE '^- \[x\] FU-' F)" = "$(grep -cE '^- \[x\] FU-.*done=PR#[0-9]+' F)" ]
> ```
> `[0-9]+` 已经排掉了正文里字面的「done=PR#n」；再加 ` *$` 只会**禁止任何尾部标注**
> （如 `done=PR#142(仅子进程支)`）并误报。在最初那个真 bug 上两种写法同样给 1 ——
> **锚从来没有承重。**（PR-Daemon 在 #144 用四棵树做控制实验推翻了它自己开的处方。）

- [ ] FU-1 · B · src=PR#140 · 2026-08-23 · rekey 失败后 record_os_partition 撞的是 (org_id,space_id) UNIQUE，ON CONFLICT(owner_key) 接不住 → 冒出裸 Sqlx 错误而非 typed Conflict；运维日志因此说不出「有陈旧 v1 行占着这个 (org,space)」
- [ ] FU-2 · B · src=PR#140 · 2026-08-23 · open_memory_base (server.rs:243) 在记忆库打不开时只 warn! → daemon 可能长期在「无模块记忆也无会话记忆」下运行。提到 error!
- [ ] FU-3 · B · src=PR#140 · 2026-08-23 · sweep 的安全性论证只讲进程内挂载顺序；真正挡跨进程的是 try_acquire_singleton() 与临时 daemon 走 open_memory()，两处注释都没写。且 KvStore::open 是 public，别的进程仍可 attach 同一文件
- [ ] FU-4 · B · src=PR#140 · 2026-08-23 · 首次插入时无人检查 owner_key 是否真的编码了所声明的 (org_id,space_id)。carol 可抢先用 alice 未创建的 key 在自己 org 下 record，alice 从此永久拿不到该分区且无修复路径（可达性为零）。要么写成 record_os_partition 文档里的显式非属性，要么把一致性断言上提到 OsMemoryCatalog::record
- [ ] FU-5 · B · src=PR#140 · 2026-08-23 · Conflict 消息只打呈递的身份、不打已存的 → guard 触发时日志说不清是哪个 org 占着那一行
- [ ] FU-6 · B · src=PR#141 · 2026-08-23 · spec.md:31 usr: 与 os: 前缀的不相交性依赖模块名为 ASCII；T1.2.1 已要求补交叉积测试，确保测试真的覆盖非 ASCII 输入
- [x] FU-7 · B · src=PR#141 · 2026-08-23 · git-guard.sh 的 --allow-trunk 只查 classic branch protection 端点，对 ruleset 保护的分支误判为无保护。修法：classic 报 Branch not protected 时回落查 /rules/branches/{branch}。属 pilot skill，不在本仓库 · done=PR#141
- [ ] FU-8 · **决策待定** · src=reference-notes/macro.md §11.2 · 2026-08-25 · **T1.1.1 的 `Decision{allow,reason}` 是可被忽略的返回值** —— 调用点写 `let _ = decide(..)` 就绕过了，而 architecture.md 自己说「贵的是调用点」。Macro 的 `EntityAccessReceipt<T>`（私有字段 + 唯一会校验的构造函数 + 领域方法要求收据）把「忘了检查」变成编译错误。**建议改成 `authorize() -> Result<AccessGrant, Denied>` + `lend(grant)`。这条要在 T1.1.1 开工前拍板**，之后改就是「另一个量级的工作」。未验证：F8 全部 lend 调用点是否都拿得到 grant
- [ ] FU-9 · B · src=reference-notes/macro.md §11.1 · 2026-08-25 · 「模块永远拿不到 KvStore」今天靠私有字段 + 复审纪律。Macro 用 cargo feature 把层间依赖变成构建保证。修法：agent24-memory 分 root/scoped feature，模块侧只开 scoped，KvStore 在那个构建里不存在
- [ ] FU-10 · B · src=reference-notes/berd.md §10.2-D · 2026-08-25 · **契约漂移 CI**：`os list` 输出形状变了没有任何东西会红；SPEC-ME-FOLLOWUPS F4a 抓的「lint:openapi 抓不到少写端点」是同一个病的另一个病灶。修法学 berd 的 `generate-berdctl-contract --check`（从命令模块生成契约、与签入的对比）。berd 是 Apache-2.0，脚本可直接借用 + 保留版权头
- [ ] FU-11 · **需先核实** · src=reference-notes/berd.md §10.2-F · 2026-08-25 · berd 把「CLI 校验只是便利、任何同用户进程都能绕过 CLI 直接打 broker」写成契约，信任边界放在最内层 registry。**待查：agent24d 的 HTTP 面是否独立校验了 `agent24 os` 传来的输入？** 若否是真实的洞。核实前不得当成已知缺陷陈述
- [ ] FU-12 · B · src=reference-notes/macro.md §11.3 · 2026-08-25 · frecency（frequency × recency）作为一等排序键 —— 语义检索抓不到「我最近反复碰过的东西」这一维。F1.3 之后对记忆召回质量是便宜的大改进
- [ ] FU-13 · B · src=reference-notes/berd.md §10.3-I · 2026-08-25 · oMLX / 模型权重的版本管理今天靠约定。学 berd 的 goose-backend.lock.json：lockfile 钉死 + 缓存与 lockfile 不一致时**直接 fail，不静默回退**
- [ ] FU-14 · **决策待定** · src=reference-notes/berd.md §9 · 2026-08-25 · **ME-3（进程外 Provider）开工前需一条 ADR 裁决「自定义协议 vs ACP」**。Berd 与 Macro 两个互不相干的团队都用 ACP 做「壳 ↔ agent 运行时」边界。理由不是从众，是互操作。未验证：ACP 的能力覆盖度、与审批门/ScopedMemory 的契合度 · **已裁决(2026-09-02,ADR-031)**：补做了那次验证——ACP 是 JSON-RPC 2.0,有 `session/request_permission` 反向审批、有 `_`/`_meta` 扩展机制,但**对象模型是会话与提示,不覆盖模块/路由/清单/作用域记忆,记忆更是明确不在其范围内**。结论是**按缝分别裁决**:缝 B(内核↔领域 OS,即 ME-3)不采用 ACP(采用等于全部语义落在 `_agent24/*` 扩展里,买到的是信封不是互操作——而互操作是推荐它的唯一理由);缝 A(壳↔agent 运行时)仍是强候选,另行立项。MCP 同理不采用为模块协议(SPEC-MD-ME 的「经 MCP/协议」是立项时的猜测,ADR-031 改正之)。设计文档:`SPEC-ME3-OUT-OF-PROCESS.md`。**本条待 PR 号落定后翻 [x]**
- [ ] FU-15 · B · src=reference-notes/berd.md §10.1-A · 2026-08-25 · **建 `docs/laws/`**：把 architecture.md 的「不可动摇的边界」+ SPEC-ME-FOLLOWUPS F1 的「不得声称的话」抽成编号的 MUST/MUST NOT 条款，并在 PR 模板加「本 PR 影响哪几条法律」。F1/F8 二十余轮复审抓到的几乎全是「措辞比机制强」= 法律与实现不一致，今天靠人肉记忆发现
- [x] FU-16 · **A · F5 阻塞级** · src=reference-notes/proma.md §1 · 2026-08-28 · **两个渠道桥都不处理机器睡眠/唤醒**（已验证：`grep -rn powerMonitor|suspend|unlock-screen apps/desktop/src packages/*-bridge/src` 零匹配）。macOS 睡眠后 TCP 长连接常处于「不报错但永不收消息」的僵尸态 → 错误路径不触发、重连不启动、launchd 不会救。**F5 是「Mac mini 连跑 7 天」，必然多次休眠。这是最可能让泡测在第二天早上静默失效的一条。** 修法：桥进程加定时健康检查 + 主动探活重连（不必依赖 Electron powerMonitor，桥是独立 Node 进程）。参考 Proma 的两段恢复延迟 [1.5s, 10s]。**⚠️ #142 交付的不是这条处方**：它加的是 agent-speaker 子进程的 execFile deadline(60s + SIGKILL)，关的是同一个失效模式(轮询循环因子进程挂起而永久停摆)，但**没有**定时健康检查、也没有主动探活重连 —— 在 #142 的 head 上重跑本条自己的判据 `grep -rnE "powerMonitor|suspend|unlock-screen"`，与 base 一样只有 1 处同一行无关注释。选那条路是对的(更少的活动部件关掉了同一个洞)，但原处方仍未实现,若将来发现 deadline 不足以覆盖(例如子进程本身健康却收不到 relay 消息),这条要重新打开 · **⚠️ 范围已收窄(2026-09-02,PR-Daemon 在 #144 推翻了它自己上一轮的背书)**：#142 交付的 execFile deadline 只关上了**子进程挂起**那一支。**本条描述的主要失效模式没有被关上** —— `inbox()` 跑的是 `history inbox`,读的是 **daemon 填充的本地库**(`speaker.ts:190` 注释自陈)；睡醒后僵尸的若是 **agent-speaker 守护进程那条 relay 连接**,CLI 依然秒退 exit 0 返回空列表,没有任何东西挂起,deadline 永不触发,`pollOnce` 正常返回,失败计数器数的是 catch 而这里没有异常。**空列表与「本来就没有新消息」不可区分。** 未交付的那一半 → **FU-32** · done=PR#142(仅子进程支)
- [x] FU-17 · **A · F5 阻塞级** · src=reference-notes/proma.md §5 · 2026-08-28 · `packages/wechat-bridge/src/main.ts:36` 的 `wechat-sessions.json` 用裸 `writeFileSync`，无原子写。断电/强制关机会截断该文件 → 所有微信用户的会话映射丢失。修法：write-to-temp → rename（POSIX 原子）+ .bak 回退。**五行代码** · 已交付于 PR#142 · done=PR#142
- [ ] FU-18 · B · src=reference-notes/longhorizon-harness.md §10.1-A · 2026-08-28 · **agent loop 没有「打转」检测**。C5 调度器有「连续失败 5 次自动禁用」但那是 schedule 级；loop 内部「反复产出无效计划 / 反复被拒 / 空进展」没有任何计数器。LongHorizon 的 `repeated_failure` 是唯一一个来自趋势而非单轮结果的人工介入触发 —— 打转是长程任务最贵的失败模式，且单轮看不出来。不需要额外 LLM 调用
- [ ] FU-19 · B · src=reference-notes/longhorizon-harness.md §10.1-C · 2026-08-28 · LongHorizon 的 `_open_nofollow`（`supervisor/control_bus.py`）拒绝跟随符号链接打开文件 —— 直接对上 SPEC-ME-FOLLOWUPS **F6**（模块目录 symlink 安全今天只有检查不是保证）。**MIT 许可证，可直接借用 + 保留版权头**
- [ ] FU-20 · **需先核实** · src=reference-notes/longhorizon-harness.md §10.2-E · 2026-08-28 · **LLM 输出解析遇到畸形输入时默认落在哪一侧？** 待查三处：Guardian 的 `{risk_level, rationale}`、MD-4 的 candidate 抽取、摘要压缩。LongHorizon 的 auditor 控制头缺失/畸形一律降级 `suspect`（fail-closed），且另有一个只修格式的专门轮次。核实前不得当成已知缺陷陈述
- [ ] FU-21 · **需先核实** · src=reference-notes/proma.md §10.2-C/E · 2026-08-28 · ① MD-7 知识层的层级 markdown 合并向上走几级、符号链接怎么处理、有无大小上限（Proma 明令禁止祖先目录规则发现 —— 那是提示词注入路径，并配 realpath + 64KB/128KB 上限 + 内容哈希）；② 微信 `bot_token` 过期后有无重新登录路径（`login()` 只在 `main.ts:57` 启动时调一次）
- [ ] FU-22 · B · src=reference-notes/longhorizon-harness.md §10.2-D · 2026-08-28 · **角色能力边界是与 `risk_class` 正交的第二个维度**。H9 explorer 已是「结构性只读」但那是一个特例，不是可复用的角色-工具矩阵。LongHorizon 给每个角色发不同工具集：manager 连 Bash 都没有、auditor 有 computer MCP 但无写工具、executor 禁 Agent（禁递归）；并用 `path_deny_rules` 把 harness 自身路径对 agent 隐藏
- [ ] FU-23 · **决策待定** · src=reference-notes/proma.md §10.3-H · 2026-08-28 · **开源/商业能力边界表**。MISSION.md 写了 MushroomDAO 开源 + HyperCapital 商业，但「哪些能力属于开源侧、哪些属于商业侧」今天没有工程边界也没有一张表。Proma 划的线（产品完整开源，商业化托管额度与组织级 Skill 分发）与 Mycelium 价值观相容。**Cos72 商业化之前先划线再写代码 —— 这是产品决策，需用户拍板**
- [ ] FU-24 · **决策待定 · 不排期** · src=reference-notes/longhorizon-harness.md §11 · 2026-08-28 · **要不要引入独立 Auditor 角色**。Agent24 的 agent loop 今天没有任何独立验证环节（工具执行完，模型自己判断成功与否）。但直接抄有三个坑：① 每轮多一次完整 LLM 调用，与本地小模型优先的定位冲突且 7B 能否支撑 fail-closed 未验证；② 与 H8 plan mode 语义重叠（做前批准 vs 做后验证），堆一起审批疲劳翻倍，可能变成 F2 那种「两套并存」的债；③ 它假设任务有可独立观察的外部状态，而微信渠道进来的对话式 run 没有可审产物。**建议 M4 Cos72 workspace 之后再议**（那时才有稳定可观察的交付物）
- [ ] FU-25 · B · src=reference-notes/proma.md §10.3-G · 2026-08-28 · **Skill 分发的完整机制 = 两半**：berd 的 `distro/` 分发缝解决「私有物怎么叠加」，Proma 的 frontmatter `version` 比对解决「已分发的怎么升级」（只有版本号变高才覆盖用户工作区，不比内容不比时间戳）。Cos72 给社区分发 Skill 两条都要。M5
- [ ] FU-26 · B · src=reference-notes/proma.md §10.3-I · 2026-08-28 · **记忆的可见性**：记忆底座有事件/断言/巩固/知识层，但用户看不见它记了什么、也无从纠正。M1 F1.3 把会话写进 EventLog 之后会立刻变成真问题。Proma 的做法是记忆变更由 watcher 捕获并在 UI 提示刷新。M4/M5 考虑
- [ ] FU-27 · B · src=laws/CONTEXT.md L-CTX-4 · 2026-09-02 · MD-8 TaskTrace(工具日志落 refs/*.md) 与 MD-2 ArtifactStore 共用同一套内容寻址存储，**「哪些是任务产物、哪些是运行记录」今天没有类型层面的区分**。等 FU-24(是否引入 Auditor 角色)裁决后再决定要不要给 L-CTX-4 一个机制
- [ ] FU-28 · B · src=specs/SPEC-SKILL-DISTRIBUTION.md §3.2 · 2026-09-02 · **Skill 版本递增的 CI 门**：改了 skills/<name>/** 但 SKILL.md 的 version 没变高 → CI 必须红。与 A6 零漂移门同理（忘了不会立刻出错，只让老用户收不到升级，且开发机永远是全新安装所以没人会发现）。这是 F4a 那个病的第三个病灶。M5 / SD-3
- [ ] FU-29 · B · src=Codex 复审 High-3 · 2026-09-02 · **Nostr 回复是 at-most-once，且超时时投递状态不确定**。inbound.ts 先标 seen 再执行，reply() 吞异常不重抛（重抛会让整个 run 连同副作用重跑一遍，只为修一个投递问题）。子进程超时被 SIGKILL 后**我们不知道消息发没发出去** —— 盲重试可能重发，盲丢弃可能丢消息。本轮只做到「能区分 + 日志说清楚」(SpeakerTimeoutError.indeterminate)。要变成 at-least-once 需要**持久化 outbox + 发送侧幂等键**（Nostr 侧可用 event id 去重）。属设计改动，不属活性修复
- [ ] FU-30 · B · src=Codex 复审 Medium(第二轮) · **判断已于 2026-09-02 更正** · 两个 wechat-bridge 进程仍会在 `rename` 上竞争(last-writer-wins)。**更正**:我此前写「本 PR 对并发场景是严格改善」——不成立。temp+rename 确实消除了撕裂的主文件,但第二轮我按「PID 不同 = 孤儿」清扫临时文件,引入了一个本分支之前不存在的失效模式(一个进程的 load() 会删掉另一个活写者的在写临时文件)。Codex 第三轮指出这一点,已改用年龄判据修掉。**今天双进程仍不是受支持的配置**(两个进程会对每条消息各回一次),若将来支持多实例需要 O_EXCL 锁文件
- [ ] FU-31 · B · src=Codex 复审 High-2 · 2026-09-02 · macOS 的 `fsync` 不保证盘自身刷新写缓存，需要 `F_FULLFSYNC`（Node 不暴露）。当前注释已如实写明「只关上了 page cache 那个窗口」。若将来要对断电做更强承诺，需经 native 调用或换存储层。**不得**在任何文档里把现状写成「抗断电」
- [x] FU-32 · **A · F5 阻塞级** · src=PR-Daemon 在 #144 推翻自己上一轮的背书 · 2026-09-02 · **入站 relay 静默：桥无法区分「收件箱为空」与「relay 连接已死」**。这是 FU-16 原本描述的**主要**失效模式,#142 没有关上它(只关了子进程挂起那一支,见 FU-16 收窄说明)。今天全仓**零活性判据**：`grep -rniE "heartbeat|健康检查|liveness|watchdog|lastSeen|staleness" packages/*/src | grep -v .test.` 唯一命中是生成文件 `api-client/src/openapi.d.ts` 里 `/health` 的注释,不是桥的机制(正对照:同范围 `SpeakerTimeoutError` 命中 7,判据非恒空)。修法不能是「再加一个超时」—— 需要一个**正向的活性信号**:要么让桥直接探 relay(而不是只读 daemon 的库),要么读出 daemon 侧的 last-seen/连接状态并对陈旧度设阈值。**F5 泡测的验收必须包含这一条**:睡眠→唤醒后,对端发一条消息能在 N 分钟内收到,而不只是「进程还活着」 · **交付说明(2026-09-02)**:处方里的两条路都走不通 —— 「读 daemon 侧 last-seen」**没有 last-seen 可读**(hyphae@98834db 的 `internal/daemon/daemon.go` 无 status 子命令、不写心跳、`watchOneRelay` 第 309-312 行直接吞掉拨号错误),「桥直接探 relay」违反 G3。实交付的是**第三条路**:桥通过同一个 CLI 给**自己的 npub** 发 canary,靠本地库 `INSERT OR REPLACE` 把 `is_incoming` 从 0 翻成 1 来判定 —— 只有 daemon 真的从 relay 把这条事件拉回来,行才会对桥可见。这是端到端的**正向**信号(relay 可达+daemon 在跑+订阅+解密+落库+桥可读),且桥不开任何 relay 连接。fail-closed:只有**本进程发出且仍在追踪**的 canary 能确认,每个 id 只确认一次(按文本认会让窗口里的旧行永久假活)。degraded 时 stderr 报警 + 写 `~/.agent24/nostr-bridge-health.json` 供泡测 `cat`。上游那两条盲区另开 FU-33 · done=PR#147
- [ ] FU-33 · **B(上游 hyphae,非本仓)** · src=为 FU-32 读 agent-speaker 源码时发现 · 2026-09-02 · **daemon 自己也看不见自己的入站通路死没死**,两处:(1) `internal/daemon/daemon.go:309-312` `watchOneRelay` 对 `RelayConnect` 失败是**裸 `return 0`** —— 不打日志、不计数、不落盘,relay 连不上和 relay 没新消息在**它自己的 stdout 里**都是同一行 `Watching... (no new messages)`;(2) 全进程无 status 子命令、无心跳文件,所以任何下游(桥、launchd、人)都没有 last-seen 可读。FU-32 因此只能在**桥侧**用 canary 自造信号 —— 能用,但活性判定本该由持有连接的一方给出。修法:daemon 每轮把 `{last_relay_ok_at, last_error, relays[]}` 写一个心跳文件 + 加 `hyphae daemon status --json`;桥随后可以把 canary 降级成兜底。**另建议把 FU-32 依赖的那条契约作为测试贡献上游**(发出→`is_incoming=0`、daemon 拉回→同一主键 REPLACE 成 `is_incoming=1`、id 等于 `agent msg --json` 返回的 64 位 hex):我已在本机对真实 schema 跑通验证过,但上游没有任何测试守着它,一次重构就能静默作废 Agent24 的活性判据。探针留档见 scratchpad `hyphae-fu32-contract-probe.go.txt`。 **另有两条在做 FU-32 时由 Codex 挑战发现的上游缺陷**:(a) **发布早于落库的竞态** —— `agent.go:208-238` 先 `relay.Publish` 后 `StoreOutgoingMessage`,若 daemon 恰在 3s 订阅窗口内收到并写成 `is_incoming=1`,发送方随后的 `INSERT OR REPLACE` 会把它**覆盖回 0**,而 daemon 的 `seen` 已记下该 id 不会再处理 → 这条消息在本地库里永远是「出站」。修法:发布前先落库,或 upsert 时让 `is_incoming` 单调(`is_incoming = messages.is_incoming OR excluded.is_incoming`)。桥侧只能缓解(canary 抖动 + 未确认时加快重探),缓解不掉。**⚠️ 2026-09-09 实测:这条已经从「理论上可能」变成「实测挡住了 F5」。** 在本地 minirelay 上往返只要几毫秒,竞态**几乎每次都触发** —— **注意是几乎,不是每次**:实测 `sent=105 confirmed=2 lost=94`,赢面约 2%;`messages.db` 里含 canary 的行 `is_incoming=0` 130 条 / `=1` 11 条。**本条上一版写「confirmed 永远是 0」,被自己的数据推翻了。** 结论不变但理由要说准:不是「永远确认不了」,是「确认率低到判据必然失败」—— `degraded_transitions` 持续增长(实测已 2),而 F5 判据 6 要求它在本次 run 内不增长,而 daemon 日志里**每条 canary 都有 `📨 New message`** —— 即「收到了但对桥不可见」。代码位置逐字核过:`internal/messaging/agent.go:220` 先 `relay.Publish`、`:241` 才 `StoreOutgoingMessage`,而 store 是 `INSERT OR REPLACE`(`internal/storage/message.go:34`)。**修法是一行 SQL**:upsert 让 `is_incoming` 单调(`ON CONFLICT ... DO UPDATE SET is_incoming = messages.is_incoming OR excluded.is_incoming`),或先落库再发布。**在它修好之前,F5 不能用本地 relay** —— SOAK-F5 已收回那条建议。(b) **daemon 把探针消息当普通消息处理**:`daemon.go:370-378` 会为每条 canary 弹桌面通知+播音(默认 `--notify=true`,7 天约 2000 次),开 `--auto-reply` 还会各自动回一条。修法:上游为探针定义 tag,落库但跳过通知与自动回复。泡测前先用 `--notify=false --auto-reply=false` 绕开(已写进 SOAK-F5)**注意 daemon 不是长连接**:它每 30s 重新拨号、订阅 3s、关闭,所以「睡醒后长连接变僵尸」这个我们一直沿用的病因描述**对 hyphae 不成立**,真正的病因是上面这条静默的拨号失败 · **⚠️ 2026-09-10 对照实验:上面那个 2% 的读数没有复现,而且补丁在今天这套环境里量不出差别。** 做法:同一台机器、同一个本地 minirelay、同一套流程各跑一次,唯一变量是二进制打没打补丁(未打补丁的那个是从 git 还原源码后重编的,不是旧产物)。读数:**未打补丁 daemon 看到 10/30 → 10 条保住 is_incoming=1;已打补丁 10/30 → 10 条保住。两边一样。** 也就是说:(a) 补丁本身是对的 —— 单元测试在打补丁前红、打补丁后绿,带对照(纯出站消息仍为出站),另一条测试还证实 `INSERT OR REPLACE` 会把先前写入的 plaintext 一并抹掉(delete-then-insert);(b) **但今天挡住 F5 的不是这个竞态,是投递** —— 30 条只有 10 条到达 daemon,与补丁无关。**原读数 105/2/94 的坐标(哪个 relay、哪个二进制、哪一天)没被记下来,所以无法判断是环境变了还是当时量法不同 —— 这正是「读数要带坐标」那条规矩自己被违反的一次。** 结论修正:补丁保留(它修的缺陷真实存在且已被单元测试守住),但**不得声称它解除了 F5 的阻塞**;真正的阻塞见 FU-43
- [ ] FU-34 · **A · F5 阻塞级(环境,非代码)** · src=同上 · 2026-09-02 · **上游把二进制从 `agent-speaker` 改名成 `hyphae` 了**(agent-speaker repo HEAD=98834db `rename: agent-speaker -> hyphae`,`cmd/hyphae/`,go module `github.com/iDoris-ai/hyphae`)。本仓默认 `A24_SPEAKER_BIN='agent-speaker'`,且本机 PATH 上**两个名字都不存在**(`which agent-speaker` → not found)。F5 泡测机开跑前必须确认:装的是哪个名字、`agent msg/history inbox/identity list/profile publish` 的 `--json` 契约有没有随改名漂移(F4 联调是对着改名前的 7cef326 验的)。不确认就开跑 = 第一分钟就 degraded,而且原因和 FU-32 要抓的失效**长得一模一样** · **范围已收窄(2026-09-02,已在本机实测)**:二进制在 `/Users/jason/Dev/auraai/agent-speaker/bin/hyphae`(编译产物,不在 PATH 上 —— 我此前说「两个名字都不存在」指的是 PATH,说法不准)。**`--json` 契约没有随改名漂移**,四条逐条验过:信封仍是 `{"ok":true,"data":...}` / 失败是 `{"ok":false,"error","message"}`;`identity list --json` 返回裸数组;`history inbox` 认 `--as/--limit/--json`;`agent msg` 的 `--from/--to/--content/--relay/--encrypt` 与 `profile publish` 的 `--as/--json-file/--relay` 都在。**剩下三件仍未做**:(1) 装上 PATH 或给桥设 `A24_SPEAKER_BIN=/Users/jason/Dev/auraai/agent-speaker/bin/hyphae`;(2) 建 identity(现在 `identity list` 是空的,所以 `history inbox --as agent24` 返回 `identity 'agent24' not found`);(3) `agent msg` / `profile publish` 的**运行时**行为(真发一条、真发布一次)仍需 relay 在场才能验 —— 这两条只能在 F5 起跑时确认 · **实测收口(2026-09-09,在 MacBook Pro 上把整套准备跑了一遍)**:(1)(2) 已做 —— 但撞上一条此前只在纸面上的阻塞:**`~/.hyphae/keystore.json` 是空的却带 `encrypted:true`,于是连 `identity create` 本身都要密码**,非交互直接 `failed to read password: operation not supported by device`(`commands.go:57` 对**每个** identity 子命令都先 `if ks.Encrypted { PromptPassword }`),而 keystore 路径**无法覆盖**(`keystore.go:18-28` 只用 `os.UserHomeDir()/.hyphae`,无 env 无旗标)。因为那个 keystore **零个 identity**,移开重建即可(已备份);全新 keystore 不带 `--password` 落成不加密。**若 keystore 里已有真 identity,这条就是硬阻塞 = R3。** 顺带更正本仓 SOAK-F5 的一处不实:`--auto-reply` 的默认值**已经是 false**,不需要显式关(`--notify` 确实默认开)。(3) **仍未验完,而且发现了更前面的东西**:见 FU-38
- [ ] FU-35 · B · src=Codex 第九轮(FU-32 收口时的停止线判断) · 2026-09-02 · **`soak-monitor.sh` 的自动 verdict 只覆盖 F5 判据 1、2、6**,而且是每 5 分钟一次的**离散采样** —— 短于采样间隔的 daemon/scheduler 瞬态会漏采。判据 3(无人工干预)、4(RSS 趋势)、5(渠道首尾端到端)仍要人工核验;**不能只凭 `RESULT: PASS` 就把 F5 标 done**。另:监控进程自己没有 supervisor / 断点续跑 / run id,主机重启或它自己异常退出都会留下不完整证据(必须重跑),而且日志是**追加**写的 —— 每轮要用新的空日志路径,否则多轮 JSONL 会混在一起,事后判读会串。
- [ ] FU-36 · B · src=PR-Daemon 在 #147 转来的 Codex 补扫,我已独立核实 · 2026-09-02 · **F5 判据 2 的「调度器活着」用的是存在性判据,不是活性判据** —— `soak-monitor.sh:468` 的 `schedule_min` 取的是 `/schedules` 数组**长度**,而 `nullnext`(enabled 但 `next_run_at==null`,即已触发完的一次性任务)按第 466-467 行的注释是**有意只记不判**。合起来:一轮泡测里唯一那条 schedule 是个跑完的一次性任务,`schedule_min>=1` 成立、`overdue=0` 成立,判据 2 通过 —— 而调度器整周可以什么都没做。`last_run` 已经采进 JSONL 但不参与判定。**这正是 FU-32 那个病在判据 2 上的同形**:存在性证明不了活性。修法与判据 6 现在的做法同构 —— 要求 `last_run` 在本轮内**前进**(而不是「有值」),并把「本轮从未前进」判失败;注意区分「没有到期的任务」和「到期了没跑」,否则会误杀合法的低频日程。属既有逻辑,非 FU-32 引入,故不在 #147 内扩范围
- [x] FU-37 · **B · 台账矛盾** · src=写 README 里程碑表时发现,PR-Daemon 在 #149 独立确认 · 2026-09-08 · **两份进度台账对 M-D 的说法互相矛盾,且都不完全对**。`docs/specs/SPEC-MD-ME.md:9` 写「MD-1..8 全交付」;`docs/specs/TASKS.md:217-219` 写 MD-1/2/3 `pending`。而按代码,两边都不准:crate 层的库原语与迁移**确实在**(`agent24-memory` 有 artifact/assertion/condenser/consolidator/event/eval 等模块),但 **daemon 没有端到端消费**它们 —— ① 巩固只有调用方驱动的 `run_once`,没有 daemon 后台循环,默认 synth 只按事件数量生成文字;② 新 condenser 未接管现有 session 路径(其文档自陈现用的 `CanonicalSession` 仍会丢弃折叠后的原文);③ MD-6 只有 `Embedder` seam 与测试用 embedder,真实 `OmlxEmbedder` 不存在。README 已如实降为 🟡 并写明「两份文档互相矛盾,待对账」,但**矛盾本身没修**。修法:先按代码定出每个 MD-x 的真实状态(交付=库原语 vs 交付=已接线),再让两份文档指向同一个状态源 —— 今天 TASKS.md 自称「loop 的唯一工作来源与状态文件」,而 SPEC-MD-ME 的进度行事实上也在被人读。**这是 FU-15(建 docs/laws/)想解决的那类问题的一个实例**:两处措辞都比机制强,且靠人肉记忆才发现 · **交付说明(2026-09-09)**:核实后发现比「两份文档互相矛盾」更糟一层 —— **`TASKS.md` 的 M-D 表是 2026-07 的旧编号旧计划**(只有 MD-1..4 + MD-X),**同一个 ID 在两处指的不是同一件事**(那里的 MD-2 是「L1 CoreMemory」,SPEC-MD-ME 的 MD-2 是「EventStore + ArtifactStore」),所以「TASKS 说 pending」根本不是在说 SPEC 那几条 —— 本条上一版把它当成同一件事的两个说法,那个判断本身也不准。而 SPEC-MD-ME **§3 的详表一直是对的**(MD-4/5/6 标 🟢 并带 🔜),撒谎的只有第 9 行那句摘要 —— 它把 🟢 压平成 ✅ 还补了「MD-1..8 全交付」。收法不是挑一边赢(挑一边会让另一半信息消失,而那一半正是别人会踩坑的那一半):① 定义两个符号(**✅=库原语交付且已被 daemon 端到端消费**;**🟢=库原语交付有测试但 daemon 未接线**)并把摘要行按详表还原;② TASKS.md 旧表换成指针并标「不再更新」,**一个事实只在一个地方写** —— 那正是漂移的根因;③ 每条 🟢 配可复跑判据**带正对照**(MD-5:`consolidat` ∩ `spawn|interval|loop`=0,正对照 `memory` 命中 4 文件;MD-6:`(struct|impl)\s+OmlxEmbedder`=0,正对照 `Embedder` 命中 2 文件 —— 裸搜 `OmlxEmbedder` 同样命中 2 文件但**全是注释**,只数命中会得出相反结论)。两条判据已自验 · ⚠️ **上面括号里那个 MD-6 正对照（裸词 `Embedder` 命中 2 文件）的写法后经 #155 判定有缺陷、已更正为同形状写法** —— 它不经过判据里 `(struct|impl)\s+` 那一段,且 `Embedder` 是 `OmlxEmbedder` 的子串、命中的正是判据要驳回的那两个文件。**本条正文保留原样作历史记录,但别照它写判据**;正确写法见 `specs/SPEC-MD-ME.md` §0 上方那段。 · **PR-Daemon 在 #152 挡下来的一条,是这条 followup 最有价值的部分**:上面那个「两个符号」的收法**自己也犯了同一个病** —— 新定义的「✅ = 已被 daemon 端到端消费」只被拿来审了 MD-4/5/6,**另外五行既有的 ✅ 没有回头按新定义复核**。用与任务名无关的量具(`KvStore` 各访问器在 `rust/apps` 里的调用次数)量下来:`.events(` 10 处、`EventLog` 4 处是**唯一**被消费的,`.artifacts(` / `.assertions(` / `.retriever(` / `.knowledge(` / `.trace(` / `.consolidator(` **全是 0**,对应 trait 名在 `rust/apps` 也全是 0(正对照就是那 10 处 events —— 量具看得见「已接线」长什么样)。**所以今天没有任何一行够得上 ✅**;MD-2 是 🟡(EventLog 接了、ArtifactStore 没接),其余全是 🟢。教训一句话:**定义改严了,既有标记必须重算一遍,否则新定义只是给旧结论换了个更强的名字。** · done=PR#152
- [ ] FU-38 · **A · F5 阻塞级(外部依赖,非代码)** · src=2026-09-09 在本机跑 F5 冒烟时实测 · **默认 relay `wss://relay.aastar.io` 当前下线**。判据(带正对照):DNS 解析正常(`dig +short` → 172.67.215.77 / 104.21.93.214,Cloudflare)、TCP 443 通(`nc -z` 成功),但 WS 升级请求返回 **HTTP 530** —— Cloudflare 的「源站不可达」,即 relay 自己挂了,不是我们这侧的配置或网络问题。正对照:同样的握手请求打到 `relay.damus.io` 返回 200(它的 NIP-11 响应),证明量具能区分。**后果**:桥的 canary 发不出去,健康快照写 `last_error: "canary 未直达任何 relay(已入 outbox 重试队列)"`,`state` 停在 `starting` —— **F5 判据 6 在这个 relay 上永远不可能通过**。要做的:① 起跑前把 relay 可达性做成一条**前置检查**(现在没有任何东西在起跑前验它,桥要等到第一条 canary 超时才知道);② 决定 F5 用哪个 relay —— 公共 relay(damus 等)更真实但受第三方可用性摆布,`~/Dev/auraai/agent-speaker/bin/minirelay` 本地起一个则隔离了外部故障但测不到真实网络;③ **`relay.aastar.io` 是 iDoris 自己的 relay(agent-speaker-relay),它下线本身要报给上游** —— INTERFACES.md 把它列为「核心」依赖
- [ ] FU-39 · **A · F5 阻塞级** · src=2026-09-09 跑 F5 冒烟时自己踩到 · **两个桥进程会抢同一个健康快照文件,而它的失败症状会把人引向完全错误的诊断**。实测:切换 relay 时我以为杀掉了旧桥(`pkill -f "tsx src/main.ts"`),实际只杀掉了包装进程 —— 真正的 worker 命令行是 `node --require .../tsx/dist/preflight.cjs`,不含 `tsx src/main.ts` 这个串,存活了下来。于是两个桥同时:① 都往 `~/.agent24/nostr-bridge-health-<identity>.json` **原地覆盖写**(last-writer-wins);② 都往同一个日志文件写(日志里出现半行乱码 `��人收)` 就是交错写的证据);③ 各自的 canary 计数交替出现在同一个文件里。**后果最严重的是 `generation`**:它在两个 UUID 之间来回跳,而 `soak-monitor.sh` 把 generation 变化判为「账本被重置」并判失败 —— 那条诊断指向的是「有人删了健康文件」,操作者会去查一个根本不存在的原因,而真因是「有个僵尸桥还活着」。**注意 canary 的确认判据本身没被污染**:`observe()` 只确认**本进程发出且仍在追踪**的 canary(fail-closed 设计),所以两个桥不会互相「确认」对方的 canary;被污染的只有那个共享的证据文件。修法二选一:① 桥启动时用 `O_EXCL` 锁文件或把 pid 写进健康快照并在启动时检查(同 identity 已有活着的写者 → 拒绝启动并打印那个 pid);② 至少让 soak-monitor 在 generation 变化时把「可能有第二个桥在写」列进诊断文案 —— 现在的文案只说「账本被重置」。**这与 FU-30(两个 wechat-bridge 进程在 rename 上竞争)是同一族**:双进程今天不是受支持的配置,但**没有任何东西阻止它发生**,而这次它让一整轮冒烟的证据作废。顺带:`docs/SOAK-F5.md` 已写「两台机器同时跑必须各用各的 identity」,但**同一台机器上的僵尸进程**这条没写,而它更容易发生
- [ ] FU-40 · **B · CI 盲区** · src=PR-Daemon 在 #160 · 2026-09-10 · **没有任何 CI job 跑 `cargo doc`,所以文档链接断裂对每一道闸门都不可见**(`fmt` / `clippy -D warnings` / `cargo test` 一条都看不见)。**读数与口径一起写,否则下一个人对不上账**:口径 = `RUSTDOCFLAGS="" cargo doc --workspace --no-deps` 输出里各 crate 的 `generated N warnings` **求和**;**不要用 `grep -c '^warning:'`** —— 它把每个 crate 的汇总行也算进去(实测同一棵树上是 20 而不是 13,差的 7 条正是汇总行)。读数:在 `2c7532e`(#160 修完后)是 **13** —— agent 2 · domain 2 · memory 5 · models 1 · policy 1 · store 1 · tools 1;在 `4a9d3b9`(#160 之前的 main)是 **15**。#160 引入 2 条 + 随文件搬进新 crate 1 条 + 顺手清掉 #156 引入的 1 条(`ManifestUnsupported` 链私有 `RawManifest`),共修掉 4 条。**判据(带正对照)**:`RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` 应为 0 —— 正对照是**今天它是 13**,证明这条命令不恒空。修法:先清那 13 条,再把它加进 CI 的 Rust job。**注意两类警告性质不同**:「unresolved link」是纯文档债;而「public documentation links to private item」**只在某个东西变成公开 API 之后才存在** —— #160 那条就是搬 crate 搬出来的(在二进制 crate 里 rustdoc 根本不生成它的文档),所以这类会随着抽 crate / 提升可见性**新长出来**,清完不等于以后不长
- [ ] FU-41 · **B · ME-3b 前必须闭** · src=Codex 挑战 @ #161 · 2026-09-10 · **ephemeral packages root 的名字不再可猜(#161 加了 per-process 随机 tag),但仍然没有任何所有权/权限检查**。今天的形状是一个**纯路径函数** —— 它不创建目录,所以它没有位置去做 `mkdir` 独占创建、`chmod 0700` 或「这个目录的 owner 是不是我」。剩余风险面:共享 `/tmp` 的 Linux 上(macOS 每用户私有 `$TMPDIR`,不适用),攻击者若**猜中**该路径可预置一个包目录,ephemeral daemon 会扫到它。今天的代价只是污染拒绝列表;**ME-3b 之后包 manifest 能指定 spawn 的进程,那时它就是一条执行边界**。修法:把「解析路径」与「取得可用的 root」拆成两件事 —— 后者做独占创建 + 权限校验 + 校验失败就拒绝启动,不要静默换一个目录。**判据(带正对照)**:在一个预先由别人创建、mode 0777 的 ephemeral root 上启动 ephemeral daemon → 必须拒绝;正对照 = 自己创建的 root 上启动正常。**不要用「路径不可猜」当作已修** —— 不可猜降低命中率,不改变命中后的后果
- [ ] FU-42 · **B · 3b-2b 落地前必须钉** · src=复审 A2 @ #164 · 2026-09-10 · **`MAX_FRAME_BYTES = 1 MiB` 是在消费方存在之前定的,而超限设计上不可恢复 —— 它断连,不降级。** SPEC-ME3 不给数字,ME-3b-2b 的 `initialize` 还没写;若那条消息要带能力协商或工具 schema,1 MiB 未必够,而后果是死模块不是被截断的字段。**要在 3b-2b 落地之前**拿它真实的 wire shape 把这个数钉一遍,不是之后。**判据(带正对照)**:构造一条 3b-2b 实际会发的最大 `initialize`(满能力集 + 最长 manifest 摘要),量它的字节数 → 必须 < `MAX_FRAME_BYTES`;正对照 = 同一把尺子量一条明显超限的构造帧必须 ≥ 上限,证明量的不是恒真。**不要用「今天的 initialize 很小」当作已钉** —— 今天的 initialize 还不存在
- [ ] FU-43 · **A · F5 阻塞级(新发现,与 FU-33 无关)** · src=2026-09-10 做 FU-33 对照实验时量到 · **本地 minirelay 上,自发自收的消息只有约 1/3 到达 daemon**。读数:三次试验同一流程,daemon 的 `New message` 计数都是 **10/30**(打补丁与不打补丁各一次,加最初一次),**稳定在 1/3 而不是随机 —— 稳定性本身是线索,随机丢包不会这么整齐**。候选原因未验:daemon 的 `Inbox watch interval: 30s` 轮询与发送的时序、minirelay 是否保存并重放事件(可能只转发实时订阅)、发送 30 条快过订阅窗口。**后果**:F5 判据 6 按每条 canary 约 33% 确认率算,`staleAfterMs = 3×间隔` 下三次至少中一次约 70% —— **不足以支撑 7 天无人值守**。**判据(带正对照)**:同一流程下 daemon 的 `New message` 计数必须 = 发送条数;正对照 = 故意关掉 relay 时该计数必须为 0,证明这个计数会动。**不要先改 canary 间隔来掩盖它** —— 拉长间隔不改变单条投递率
- [ ] FU-44 · **B · 3b-3 落地时必须有测试** · src=复审 @ #166 · 2026-09-10 · **`initialize` 的 `auth_token` 是「一次性秘密」,而让它今天不构成 timing oracle 的不是「一次性」这句话,是「每个秘密能被测量的次数 = 1」。** 逐字节 timing 需要约 256×len 次测量。承重的是两个前提,而 `agent24-os-proto` 一个都管不了:(a) 每次 spawn 铸新 token;(b) **失败握手后不复用同一个 token 重试**。(b) 会静默失效 —— 3b-3 的崩溃重启路径若把 token 存进模块记录复用,测量次数立刻无界,**那时朴素的 `!=` 就是 oracle,而没有任何东西会告诉你这一步已经发生**。实测:`auth_token` 今天只出现在 `initialize.rs`,**生成侧还不存在**,而它的 doc 已写着 "The one-shot secret" —— 一句对尚未写出的模块的要求,不在任何清单里。**判据**:3b-3 落地时必须有一条测试回答「失败握手后重启是否复用 token」;答案若是「复用」,那就是常数时间比较从「不需要」变「必需」的那一刻。**现在不加常数时间比较** —— 今天不需要,加了反而像「已经处理过了」
- [ ] FU-46 · B · src=PR(3b-5) review · 2026-09-11 · (编号跳过 FU-45:它已被 `SPEC-ME-FOLLOWUPS.md` 占用 —— 本账本的 `followups.sh add` 只看这一个文件,取号会撞) KillPermit 不绑定到进程/那一代：写 ME-3b-3 的 Supervisor 时让它同时持有 child 与 Generation，只暴露先撤后杀的 stop(self)，并把 Launched::child 改成私有 —— 在那之前 terminate_group 的类型只保证「之前某一次 revoke 发生过」
- [ ] FU-47 · B · src=PR(3b-5) review r4 · 2026-09-11 · (编号跳过 FU-45,见 FU-46)代理交给 hyper 的请求体不计入每模块并发上限:forward 被丢弃(撤销)后、或模块提前回应不读 body 时,hyper 可能攥着这段 body 直到上游连接关闭。撤销后进程会被杀,所以那条路上是暂时的;提前回应那条路上寿命由连接决定。把许可挂到请求体上试过,更糟(不读 body 且不关连接的模块会永久饿死自己的命名空间)。要做对:限定 hyper 在响应完成后刷写剩余请求体的时长,或对上游请求禁用连接复用后在响应完成时关连接
- [ ] FU-48 · B · src=PR#175 review · 2026-09-11 · S1(#175 评审):SPEC 与 revoke() 的 rustdoc 说「撤销之后一律不再发送」比代码强 —— dispatch() 在 client.request 被 poll 之前返回,撤销若落在建连/取连接之间,hyper 仍可能把字节写给旧进程。结果上对(这类请求已 dispatched、归为 abandoned),措辞改为「撤销之后不再 dispatch;已 dispatch 的请求其字节仍可能到达旧进程,这正是它被归为结果未知的原因」
- [ ] FU-49 · B · src=PR#175 review · 2026-09-11 · S2(#175 评审):回调通道必须绑定到它完成 initialize 的那一代,不能经 Current::get() 解析 —— 否则旧进程(Draining/Revoked)发来的回调会拿去对照新的 Running 一代而被放行。在 admit_callback / Current 上写明,接线(ME3-SUP)时落实。与 FU-46 同类
- [ ] FU-50 · B · src=PR#175 review · 2026-09-11 · S3(#175 评审):代理的 legacy Client 按 authority 复用连接,重启后新一代若复用同一地址而旧进程仍在 SIGTERM 宽限期,新一代的请求可能被分到通向旧进程的 keep-alive 连接。接线时三选一:每代一个 client / 撤销时丢弃池里的连接 / 旧进程被杀之后新一代才开始接请求
