# Agent24 × Open Design 阶段进展与 Workspace 概念模型

> 快照时间：2026-09-23 04:54（Asia/Bangkok）
>
> 本文是阶段性快照；持续更新的门禁证据以 [STATUS.md](STATUS.md) 为准，冻结契约以 [ADR-002](design/ADR-002-WORKSPACE-CONTRACT.md) 为准。

## 1. 当前在做什么

当前目标是把 Open Design 作为 Agent24 的 Creative Workspace 集成，而不是直接合并两个代码库：

- Agent24 是唯一 AI control plane，负责身份、session/run、模型、工具、审批、审计和 workspace authority；
- Open Design 保留 project、conversation、Studio、Design System、Preview、Export 和 artifact lifecycle；
- Agent24 Electron 是唯一桌面 main process；Open Design 作为受管 sidecar 和隔离 WebContentsView 运行；
- 两边通过 opaque `workspace_id`、ACP 和受限 capability 连接，Open Design 不取得 Agent24 product-host authority。

因此当前阶段主要在补 Agent24 的安全契约和宿主基础，还没有开始最终 Creative 页面接线。

## 2. 阶段进度

| 工作项 | 状态 | 当前结果 |
| --- | --- | --- |
| P0 决策冻结 | PASS | ADR-001～005、依赖、风险和兼容矩阵已冻结 |
| P1 Open Design 基线 | PASS | 独立 fork、固定 pin 和无产品修改基线已通过 |
| A24-OD-00 capability authority | PASS，逐层合并中 | #226 已合入 main；#227 retarget 后等待重新审批，其余保持依赖顺序 |
| A24-OD-01 workspace registry | 约 89% | DB lifecycle + renew、allocation journal schema/约束/typed read seam 已通过；尚无 journal 写事务与安全 FS root service |
| A24-OD-02 per-run binding | PLANNED | 尚未把 Run/Session、工具、审批和事件绑定到 workspace handle |
| A24-OD-05 sidecar host | 约 70% | manager/protocol/owner/codec/frame I/O、actor policy 与 bounded Launch collections 已通过或在复核；POSIX owned pipes 已提交，真实 actor loop 未做 |
| P3 ACP bridge | PLANNED | 等待 workspace contract/binding |
| P4 Open Design adapter | PLANNED | 等待 ACP |
| P5 workspace E2E | PLANNED | 等待 registry、per-run binding 和 ACP |
| P6 Electron Creative | 基础进行中 | sidecar 基础已做；session/CSP、view、IPC 尚未接线 |
| P7～P9 产品化与发布 | PLANNED | 回归、打包、许可证、灰度和回退尚未开始 |

进度应从三个角度理解：

- 架构与安全基础约 55%～65%；
- P1～P4 第一条技术纵切约 30%～35%；
- 完整 P0～P9 产品路线约 25%～30%。

目前尚无可交付的完整 Creative UI。剩余约 24～40 个单人工程日；多 Agent 并行且无重大返工时，粗略为 3～5 周日历时间。

## 3. 接下来做什么

1. 完成 allocation journal 写事务，再实现 pinned root allocator；Windows 需真实 `FILE_ID_INFO` 门禁。
2. 完成 host lease/exact resolve core，再补 cleanup、lease drain、重试和恢复。
3. 完成 sidecar actor：owned pipes、bounded Launch decoder、launch/ready/control、exit supervision 和 shutdown。
4. 解决 legacy 多非终态 Run 与 serial lease 的迁移：durable recovery holds + 唯一 execution lease。
5. 实现 A24-OD-02：Run/Session、工具、审批、事件和审计的 per-run workspace binding。
6. 实现 `agent24 acp` 与 Open Design Agent24 runtime adapter。
7. 打通 scratch workspace → artifact/result → 显式保留、导出或 writeback 的 E2E。
8. 完成 Electron Creative 页面、隔离 session/CSP、WebContentsView、IPC 和跨平台打包。
9. 完成能力回归、SBOM/license、upstream sync、feature flag、灰度与 rollback。

## 3.1 2026-09-23 Wave 3 增量

- G1 类型所有权已收敛：allocation journal 持久化类型只属于 `agent24-store`；
  `agent24-workspace` 单向依赖并使用，禁止镜像类型和 store 反向依赖。错误归属的 #368
  已由 #371 替代并关闭，历史 head 保留审计。
- #371 的 ID/phase/failure-reason 类型与 #373 的严格只读 record decoder 已通过 SOL。
  它们仍不包含 filesystem allocator、store getter 或写事务。
- G4 从真实 workspace schema 栈而不是文档分支开始：#372 只增加 nullable workspace
  bindings 并通过 SOL；#374 的 SQLite 对抗复核发现 `TEXT PRIMARY KEY` 未显式
  `NOT NULL` 可绕过 Run/approval FK，当前正在同一 PR 修复。
  两者都不执行 legacy backfill，不改变启动恢复，也不开放 admission。
- G8 #370 初审发现基于错误字符串的分类可被输入伪造；修复后改为每次 decode 独立的
  typed context 并通过 SOL。#375 把 env 限制前移到第 65 个 raw map entry，也已通过 SOL。
- #371～#375 已全部请求外部 reviewer；截至本快照没有新的 external `APPROVED`，
  因此没有把内部 PASS 当成 merge 许可。
- 200 行仍是当前硬门禁。实践已出现人为拆分和级联 rebase 成本；建议待确认后改为
  默认不超过 200，只有不可分割的事务/状态机与其紧邻回滚、并发证据可放宽到 450，
  超过 300 强制双 SOL，500 为绝对上限。用户确认前仍严格执行 200。

## 3.2 2026-09-23 Wave 4 增量

- G1 #377 getter core 与 #380 adversarial evidence 均已通过独立 SOL exact-head review。
  当前 read seam 能严格区分 `NotFound`/DB failure、拒绝 corrupt row，并用九个写阻断
  trigger 证明 getter 不修改 allocation/workspace/audit；它仍没有 journal 写事务或 FS authority。
- G4 #374 已补显式 `NOT NULL` 和 NULL 绕过回归并通过 SOL；#379 增加五态 recovery
  model 与完整 60-case pure decision matrix，也已通过 SOL。两者仍是 dormant persistence/
  planning，不执行 backfill、恢复或 admission。
- G8 #378 bounded control reader、#381 单槽 resumable control writer 与 #382 POSIX
  owned pipes 的首个 exact head 均已通过 SOL；后者清空继承环境、一次性移交三根 pipe，
  并用 `waitid(WNOWAIT)` 非消费观察 leader。#382 后续 timing-test repair 改变了 head，旧
  review 不再适用，必须重新复核。`run()` 仍保持 inert，不能宣称 actor 完成。
- 01:48 的完整监控没有发现 external approval 或 request changes；#227 仍等待 retarget 后
  重新批准，#228/#229 继续等待父依赖，#347～#382 没有可合法合并的新增 PR。
- 200 行硬上限已让 #373 拆成 getter core + adversarial tests；#362/#370/#373/#378
  分别到达 200/200/200/200 行，#374 修复后为 199，#381 为 195。该门槛已经产生
  实际编排成本，但在用户确认弹性规则前继续严格执行。

## 3.3 2026-09-23 Wave 5 增量

- G8 #384 已在 Windows 原生 CI 证明 Job、三根 pipe、leader-exit descendant 与 exact kill
  语义，并通过新 head SOL；#386 再增加 155 行的 crate-private common owner seam。
  #386 首次 Windows clippy 找到 cfg-only `unused_mut`，同 PR 修复后原生 CI 与新 exact-head
  SOL 均已通过。#390 再以 184 行加入不消费 owner 的 common exit observation：POSIX 使用
  `waitid(WNOWAIT)`，保留正常退出码并在 reaped 阶段只读缓存；独立 SOL 已通过。
  tree-empty/reap、Launch/Owned 和 actor wiring 仍未实现。
- G1 #385 增加 inert `AllocationIntent`。SOL 发现 accessor 被放错类型，而该 PR 的 GitHub
  只有 CLA check；修复后的 head 为 153 行，完整本地验证与新 exact-head SOL 均已通过。
- G1 #387 增加 crate-private reservation core：`BEGIN IMMEDIATE`、固定冲突优先级、严格
  重读、Unix/Windows parent identity、Reserved/null invariant 与 hash-chain audit，共 195 行。
  它没有 public Store wrapper、文件系统权限或 workspace 创建；rollback/tamper/WAL/concurrency
  证据是下一堆叠门禁，完成前不能开放调用。SOL 找到 audit action 命名空间 blocker，修复后的
  新 exact head 已通过；#388 再以 131 行 test-only slice 覆盖回滚、篡改、脱敏和连接恢复。
- G4 #389 只交付 194 行的 strict hold codec 与五态/跨字段恶意矩阵。原计划把 public getter
  也压入 200 行，但会缺少 storage corruption 与 read-only getter 证据，因此主动拆开；当前
  exact-head SOL 已通过；它不执行 backfill、admission、startup mutation，也不允许修复
  markerless Active。
- 这两次问题说明 200 行能约束范围，但不能代替 exact-head 编译、平台 CI 与 reviewer；
  对原子事务硬压到 200 已经消耗正常修复余量，并让 G4 多出一层临时 dead-code seam，
  因此仍建议采用上面的受控弹性规则。

## 3.4 2026-09-23 Wave 6 增量

- G1 #391 用 125 行 test-only slice 证明两个独立 Store 在同一 WAL 文件上的竞争会按
  `allocation_id > workspace_id > relative_name` 固定优先级串行化；reopen、单条 audit、
  hash chain 与精确重放均已通过 SOL。public reservation 仍未开放。
- G8 #392 用正好 200 行增加 POSIX 单步 reap：每步最多一次 child wait 或 group probe，
  非终态与错误保留 generation permit，只有 ESRCH 确认 group 消失才释放；补齐 permit
  证据后 SOL 已通过。actor、deadline、Unconfirmed 和 Windows/common wiring 仍未接入。
- G1 取消/COMMIT 证据的首版两场景补丁达到约 312 行，在当前硬门禁下被再次拆分；
  #393 以 95 行保留最关键的“caller 取消但 COMMIT 可能已 durable”窗口，并通过真实
  commit hook、独立 reopen 和精确重放证明恢复契约。这是 200 行开始把同一事务安全矩阵
  拆成多 PR 的又一项可量化成本，弹性规则仍等待用户确认。
- G4 #394 以 197 行公开严格只读 hold getter，并删除临时 dead-code seam。无 affinity
  11 字段 storage 矩阵、语义损坏、NotFound/Database、正式 FK fixture、审计与
  `total_changes()` 只读证据均已通过 SOL；writer、backfill、admission 与 startup 仍未实现。
- G1 #395 以 58 行证明 COMMIT hook 主动拒绝会完整回滚，移除 hook 后同一 intent 可成功；
  #397 再以 122 行和 100 次压力运行证明 INSERT 后、COMMIT 前取消会经同连接 FIFO barrier
  完成 rollback，reopen 后仍为零写入。两片均保持 test-only，public wrapper 仍未开放。
- G8 #396 的首个 Windows head 被原生 CI 找到 `GenerationOwner` 编译缺口；独立修复提交
  改为在 `OwnedProcess` 上探测 Job tree。新 exact head 的 CLA 与 Windows CI 已绿，PR
  保持 200 行和 draft，等待新 SOL 结论后才请求外部 review。
- G4 下一门禁已收敛为 crate-private Ready mutation plan，再依次做同事务 CAS/adversarial/
  approval 组合 seam；在 persisted evidence、终态释放、sweep 保护和 serial admission
  同时成立前，不公开 writer，也不把现有 daemon `resume_run` 接到恢复路径。
- G4 #398 已用 133 行交付上述纯内存 Ready plan；SOL 先发现 caller approval mismatch
  被误报为持久化损坏，修复为静态 `InvalidValue` 后 exact-head 通过。
- G1 #399 再用 120 行真实 `create_workspace` 证明 legacy/create 与 journal reservation
  的非对称顺序；双 Store WAL/reopen、末尾 workspace guard、audit 后重读与 public wrapper
  仍按五片计划串行推进，不能把 journal uniqueness 当作 legacy create 约束。

## 3.5 2026-09-23 Wave 7 增量

- G8 #400 用 144 行加入 common 层的 confirmed-empty tombstone；Windows 原生 CI 首轮在未改动
  的 owner marker/PID 测试偶发失败，精确 head 重跑全绿且 SOL 通过。它还不是 actor loop：下一片
  先实现 `Unconfirmed → Empty` 的纯 phase recovery，不重置 deadline，也不擅自 restart。
- G1 #401 用 157 行补齐 legacy create 与 journal reservation 的双 Store WAL 顺序证据；#403
  再用 115 行在 audit 之后、COMMIT 之前、同一 `BEGIN IMMEDIATE` 事务中重查
  `workspaces.id`，两个真实 trigger 场景都验证 allocation/audit 一起 rollback 且可重试。
  #404 用 59 行在 audit INSERT 后再次 strict reread allocation，语义漂移和 BLOB storage
  trigger 均会整体 rollback；#406 最后以 84 行公开 database-only reservation API，并由
  crate 外测试冻结“不会注册 workspace/不会 materialize filesystem、精确 replay 静态冲突且
  不重复 audit”的契约。#403/#404/#406 均已通过内部 exact-head SOL。
- G4 原 Ready writer 在 195 行内只能容纳 FK-off happy fixture，且没有完整 strict reads、SQL
  分类、audit/reread failure rollback 与边界矩阵；SOL 因此拒绝发布。重新拆分后的 #402 用
  193 行先交付真实 FK 链和 run/approval 严格事务事实读取，并证明只读不增加
  `total_changes()`。#405 再以 137 行加入 hold transaction reader 与恶意
  storage/enum/missing/BINARY 矩阵；SOL 首轮挡住 approval 大小写和两个 `id` storage 假覆盖，
  修复后复审通过。writer 在读取、回滚与边界门禁全过以前继续 dormant。
- 05:30 ICT monitor 没有发现可合法合并的变化：#227 仍为
  `REVIEW_REQUIRED/BLOCKED`；#228/#229 虽已外部 APPROVED，但受 #227 依赖阻塞，禁止越序；
  #383–#403 的精确 head 未漂移，已观察到的 checks 均绿，仍等待外部 review。

### 200 行门禁的当前建议（等待用户确认）

目前证据支持把 200 行保留为默认目标，而不是所有事务切片的绝对上限：它有效限制 scope，
但已使 G1 的单一取消/COMMIT 安全矩阵从约 312 行拆成多个 PR，也曾把 G4 的 195 行 Ready
writer 挤压成缺少真实 FK 和 rollback 证明的不可发布补丁。建议采用：

- 默认不超过 200 行；
- 无法再拆的单一事务加最小真实 schema/rollback 证据可放宽到 300 行；
- 只有同一门禁必须同时携带 concurrency 或 fault matrix 时可到 450 行；
- 超过 300 行必须两次独立 SOL exact-head review；超过 500 行禁止；
- 例外 PR 必须在正文写清不可拆原因、排除范围、回退点与预计 review 成本。

用户确认前，当前实现继续执行 200 行硬门禁。

## 4. Workspace 在 Agent24 中是什么

一句话定义：**workspace 是 Agent24 内核拥有的、可审计且有生命周期的运行资源作用域。**

它不是一个普通目录。一个 workspace 至少由这些内容组成：

- opaque `workspace_id`；
- 内部 canonical root 和不可替换的文件系统 identity/generation；
- provenance、base revision 和 writeback policy；
- lifecycle owner、state、TTL、revision；
- Run/host leases 与 concurrency policy；
- 与 run、tool、approval、event、audit 的关联。

普通 REST、ACP、renderer 和 Open Design 只看到 ID 和必要的公开状态，不看到 canonical host path。可信 registry 在绑定 Run 时，把记录变成不可由工具伪造的 `WorkspaceHandle`。

它也不是：

- VM、容器或完整 OS sandbox；
- Domain OS/module/plugin；
- 单独的一张权限票据；
- session、run 或 project 的同义词；
- 允许调用方自行提交的 `cwd`/绝对路径。

## 5. 与内核调用、OS、Capability 和模块的关系

```text
用户 / Creative principal
        │  action + resource capability
        ▼
Agent24 kernel ── registry/state/lease ── Workspace
        │                                  │
        │ Run/Session 显式绑定              │ immutable WorkspaceHandle
        ▼                                  ▼
 policy + approval ── ToolContext ── fs/shell/MCP/tool call
                                           │
                                           ▼
                                  OS 文件与进程原语

Domain module/plugin ── KernelCtx capability handles ── kernel services
        （独立扩展轴；不会因为安装或授权模块而自动获得 workspace）
```

### 5.1 与 Agent24 kernel 的关系

kernel 是 workspace 的 authority：创建 ID、保存内部 root、推进状态、发放 lease、绑定 Run，并在恢复时核对 identity。ACP、Electron、Open Design 和工具都是消费者，不能维护第二套 root 映射。

Workspace 因此是 kernel 的资源对象；`workspace_id` 类似稳定引用，`WorkspaceHandle` 才是运行时可使用的内核签发句柄。

### 5.2 与 tool/kernel call 的关系

模型发起 `fs_read`、`fs_write` 或 `shell_exec` 时，不直接把路径交给 OS。目标链路是：

```text
tool request → capability whitelist → policy/approval
             → ToolContext(workspace_id + private handle)
             → workspace-relative OS operation
```

`fs_read/fs_write` 使用 pinned directory handle 和 beneath-only 原语；`shell_exec` 的 cwd 固定为 workspace root；explorer/subagent/self-wake 继承同一个 handle。网络/MCP 工具即使不使用目录，也携带 workspace ID 用于 policy 和 audit。

### 5.3 与 host OS 的关系

OS 提供文件、目录、进程、fd/handle 和权限机制；workspace 用这些原语落实边界，但不等于 OS 本身。

v1 保证文件工具按 pinned root 运行，并把 shell cwd、审批和审计绑定到 workspace。它**不宣称**任意 shell 命令都无法访问 root 外路径；那需要后续 per-workspace sandbox/container/OS policy。改变到更强 sandbox 时，opaque ID、lease 和 Run binding 可以保持不变。

### 5.4 与 capability 的关系

Capability 回答“谁能对什么资源做哪些动作”；workspace 回答“这次运行的资源边界、状态和生命周期是什么”。两者不能互相替代。

Creative token 会绑定 workspace、principal、session/attachment、daemon/host/sidecar generation 和 TTL。即使 token 允许创建 Run，workspace 已过期、owner 不匹配或 lease 冲突时仍必须拒绝。

可把最终授权理解为：

```text
允许操作 = 身份/action capability
         ∩ workspace scope/state/lease
         ∩ policy/approval
         ∩ OS enforcement
```

### 5.5 与 Domain OS、plugin/module 的关系

Module/plugin 是“安装了什么代码和领域能力”；workspace 是“某次 session/run 可以操作哪一份任务资源”。它们是两条正交轴：

- `DomainModule` 通过经过校验的 manifest 挂载自己的路由和 store；
- `KernelCtx` 只交付获批的 events、memory、approval 等 capability handle；
- module 的持久化 data directory 不等于用户任务 workspace；
- module 获得 kernel capability，不代表自动获得任意 workspace、路径或 Run；
- 将来 module 若参与 workspace 操作，也必须收到当前 Run 的窄化 handle，不能从字符串或全局目录自行构造。

Open Design 在当前方案中也不是 Agent24 Domain OS module。它是独立 fork + sidecar + ACP consumer；若未来改为 Domain OS mount，需要新 ADR，不能悄悄混入当前实现。

## 6. 一个具体例子

用户在 Agent24 打开 Creative 项目时，Agent24 创建 `ws_...` scratch workspace。Electron host 取得短期 host lease，并把受限 handoff 给 Open Design sidecar。Open Design 通过 ACP 发起 prompt；ACP 只能把已经绑定的 `workspace_id` 传给 Agent24 Run。

模型要求写入 `index.html` 时，Agent24 校验 Creative capability、workspace active/lease、审批和 tool policy，然后使用 Run 持有的 `WorkspaceHandle` 写入 scratch root。Open Design 可以 preview/export 结果，但不能凭 `cwd` 获得其他目录，也不能直接 writeback 源仓库。最终保留、导出或发布由 workspace 外的显式 orchestrator 流程决定。
