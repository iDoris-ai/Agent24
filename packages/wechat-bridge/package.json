{
  "name": "@agent24/wechat-bridge",
  "version": "0.2.1",
  "private": true,
  "description": "F3: WeChat channel for agent24d over WeChat's official iLink Bot API. Inbound WeChat message -> agent24d run; run output + approvals delivered back to WeChat. Ported iLink protocol layer from heinu1 (bot/src/ilink), wired to the daemon's v1 HTTP API instead of spawning claude.",
  "type": "module",
  "scripts": {
    "bridge": "tsx src/main.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/qrcode-terminal": "^0.12.2",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.5"
  }
}
