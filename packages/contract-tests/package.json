{
  "name": "@agent24/contract-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Protocol contract tests — run against any Agent24 daemon (node mock or agent24d) via A24_BASE_URL",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:current": "vitest run src/current.test.ts",
    "test:v1": "vitest run src/v1.test.ts",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.5",
    "ws": "^8.21.1"
  }
}